import SwiftUI

struct StandardBackground: ViewModifier {
    func body(content: Content) -> some View {
        content
            .background(Color("AppBackground").ignoresSafeArea())
    }
}

extension View {
    func standardBackground() -> some View {
        self.modifier(StandardBackground())
    }
}