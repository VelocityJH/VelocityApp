import SwiftUI

struct StandardBackground<Content: View>: View {
    let content: Content

    init(@ViewBuilder content: () -> Content) {
        self.content = content()
    }

    var body: some View {
        ZStack {
            Color("AppBackground")
                .ignoresSafeArea()

            content
                .padding()
        }
    }
}