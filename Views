import SwiftUI

struct HomeView: View {
    var body: some View {
        NavigationStack {
            VStack(spacing: 20) {
                AppLogo()

                NavigationLink(destination: MaintenanceDashboardView()) {
                    Text("Go to Maintenance")
                }
            }
            .padding()
        }
    }
}